<Page x:Class="UnoLisClient.UI.Views.UnoLisPages.FriendRequestsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:Langs="clr-namespace:UnoLisClient.UI.Properties.Langs"
      mc:Ignorable="d"
      Background="Transparent">

    <Grid>
        <Border Background="#AA1C1C1C"
                CornerRadius="12"
                Padding="30"
                Margin="60"
                BorderBrush="#FF444444"
                BorderThickness="2">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Encabezado -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Width="40"
                            Height="40"
                            Content="⮌"
                            FontSize="18"
                            Background="Transparent"
                            Foreground="White"
                            BorderThickness="0"
                            VerticalAlignment="Top"
                            Click="BackButton_Click"/>

                    <TextBlock Grid.Column="1"
                               Text="{x:Static Langs:FriendsList.FriendsRequestsLabel}"
                               FontSize="36"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Left"
                               Margin="20,0,0,0"/>
                </Grid>

                <!-- Lista de solicitudes -->
                <ItemsControl ItemsSource="{Binding FriendRequests}" Grid.Row="1">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#33222222"
                                    CornerRadius="6"
                                    Padding="10"
                                    Margin="0,5,0,5">
                                <StackPanel Orientation="Horizontal"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Center">
                                    <TextBlock Text="{Binding RequesterNickname}"
                                               FontSize="16"
                                               Foreground="White"
                                               VerticalAlignment="Center"
                                               Width="200"/>

                                    <Button Content="{x:Static Langs:FriendsList.FriendRequestAcceptButton}"
                                            Style="{StaticResource PrimaryButtonStyle}"
                                            Width="90"
                                            Margin="10,0"
                                            Command="{Binding DataContext.AcceptRequestCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                            CommandParameter="{Binding .}"/>

                                    <Button Content="{x:Static Langs:FriendsList.FriendRequestDeclineButton}"
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Width="90"
                                            Command="{Binding DataContext.RejectRequestCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                            CommandParameter="{Binding .}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>
    </Grid>
</Page>
