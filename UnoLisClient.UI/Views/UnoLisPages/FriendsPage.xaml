<Page x:Class="UnoLisClient.UI.Views.UnoLisPages.FriendsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:UnoLisClient.UI.Views.Controls"
      xmlns:viewModels="clr-namespace:UnoLisClient.UI.ViewModels"
      xmlns:Langs="clr-namespace:UnoLisClient.UI.Properties.Langs"
      mc:Ignorable="d"
      Background="Transparent">

    <Grid>
        <Border Background="#AA1C1C1C"
                CornerRadius="12"
                Padding="30"
                Margin="60"
                BorderBrush="#FF444444"
                BorderThickness="2">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Width="40"
                            Height="40"
                            Content="⮌"
                            FontSize="18"
                            Background="Transparent"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            VerticalAlignment="Top"
                            Click="BackButtonClick"/>

                    <TextBlock Grid.Column="1"
                               Text="{x:Static Langs:FriendsList.MyFriendsLabel}"
                               FontSize="36"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Left"
                               Margin="20,0,0,0"/>
                    
                    <Button Grid.Column="2"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Width="60"
                            Height="40"
                            Content="+"
                            FontSize="15"
                            FontWeight="Bold"
                            VerticalAlignment="Top"
                            Cursor="Hand"
                            Command="{Binding OpenAddFriendModalCommand}"/>
                </Grid>

                <DataGrid x:Name="FriendsTable"
                          Grid.Row="1"
                          ItemsSource="{Binding Friends}"
                          Style="{StaticResource DataGridStyle}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          Margin="0,10,0,10">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{x:Static Langs:FriendsList.NicknameColumnLabel}"
                                            Binding="{Binding Nickname}"
                                            Width="*" />

                        <DataGridTemplateColumn Header="{x:Static Langs:FriendsList.StatusColumnLabel}" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Ellipse Width="10" Height="10" 
                                                 Fill="{Binding StatusColor}" 
                                                 Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding StatusMessage}" 
                                                   Foreground="White"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header=" " Width="130">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="{x:Static Langs:FriendsList.FriendRemovedButton}"
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Padding="6,2"
                                            Command="{Binding DataContext.RemoveFriendCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                            CommandParameter="{Binding .}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="2" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="{x:Static Langs:FriendsList.FriendRequestsButton}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            FontSize="16"
                            Padding="10,5"
                            Cursor="Hand"
                            Click="OpenFriendRequestsClick"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
