<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:UnoLisClient.UI.ViewModels.ViewModelEntities"
                    xmlns:converters="clr-namespace:UnoLisClient.UI.Converters">

    <converters:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    <converters:BooleanToVisibilityConverter x:Key="InvertedBoolToVisibilityConverter" TrueValue="Collapsed" FalseValue="Visible" />

    <DataTemplate x:Key="PlayerCardTemplate" DataType="{x:Type vm:CardModel}">
        <Grid Width="90" Height="140" Margin="2,0" ToolTip="{Binding CardData.Value}">

            <Image RenderOptions.BitmapScalingMode="HighQuality" Stretch="Uniform">
                <Image.Source>
                    <BitmapImage UriSource="pack://application:,,,/Assets/Cards/BackUNOCard.png" CacheOption="OnLoad"/>
                </Image.Source>
            </Image>

            <Image RenderOptions.BitmapScalingMode="HighQuality" Stretch="Uniform">
                <Image.Source>
                    <Binding Path="ImagePath">
                        <Binding.TargetNullValue>
                            <x:Null/>
                        </Binding.TargetNullValue>
                    </Binding>
                </Image.Source>
            </Image>

            <Border Background="#AA000000"
                    Visibility="{Binding IsPlayable, Converter={StaticResource InvertedBoolToVisibilityConverter}}" 
                    CornerRadius="5"/>

            <Button Command="{Binding PlayCardCommand}" 
                    IsEnabled="{Binding IsPlayable}"
                    Background="Transparent"
                    BorderThickness="0"
                    Focusable="False"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="ItemTemplate" DataType="{x:Type vm:ItemModel}">
        <Button Command="{Binding UseItemCommand}" 
                ToolTip="{Binding Type}"
                Background="Transparent"
                Width="50" Height="50" Margin="0,5"
                Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Image Source="{Binding ImagePath}" RenderOptions.BitmapScalingMode="HighQuality">
                            <Image.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="5" Opacity="0.5"/>
                            </Image.Effect>
                        </Image>

                        <Grid x:Name="BlockedOverlay" Visibility="Collapsed">
                            <Viewbox Stretch="Uniform" Margin="2">
                                <Grid>
                                    <Ellipse Fill="#AAFF0000" Width="100" Height="100"/>
                                    <TextBlock Text="🚫" 
                                               Foreground="White" 
                                               FontWeight="Bold"
                                               FontSize="90" 
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </Viewbox>
                        </Grid>

                        <Border x:Name="CounterBorder"
                                Background="#66000000"
                                BorderBrush="#AAFFFFFF"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="2,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                Margin="-5,-5">
                            <TextBlock Text="{Binding Count}"
                                       Foreground="White"
                                       FontWeight="Bold"
                                       FontFamily="pack://application:,,,/Fonts/#Luckiest Guy"
                                       FontSize="12"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Count}" Value="0">
                            <Setter TargetName="BlockedOverlay" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="CounterBorder" Property="Visibility" Value="Collapsed"/>
                            <Setter Property="Opacity" Value="0.7"/>
                        </DataTrigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.8"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </DataTemplate>

</ResourceDictionary>