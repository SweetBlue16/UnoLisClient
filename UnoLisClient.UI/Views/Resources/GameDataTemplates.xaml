<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:UnoLisClient.UI.ViewModels.ViewModelEntities"
                    xmlns:converters="clr-namespace:UnoLisClient.UI.Converters">

    <converters:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    <converters:BooleanToVisibilityConverter x:Key="InvertedBoolToVisibilityConverter" TrueValue="Collapsed" FalseValue="Visible" />

    <DataTemplate x:Key="PlayerCardTemplate" DataType="{x:Type vm:CardModel}">
        <Grid Margin="-35,0,0,0" VerticalAlignment="Bottom" RenderTransformOrigin="0.5,0.5">

            <Button Command="{Binding PlayCardCommand}" 
                    IsEnabled="{Binding IsPlayable}"
                    Cursor="Hand"
                    Background="Transparent" 
                    BorderThickness="0"
                    Focusable="False">

                <Button.Template>
                    <ControlTemplate TargetType="Button">

                        <Border x:Name="CardVisual" 
                                Width="100" Height="150" 
                                RenderTransformOrigin="0.5,1.0"
                                Margin="0,0,0,10">

                            <Border.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                    <TranslateTransform Y="0"/>
                                    <RotateTransform Angle="0"/>
                                </TransformGroup>
                            </Border.RenderTransform>

                            <Grid>
                                <Image Source="{Binding ImagePath}" RenderOptions.BitmapScalingMode="HighQuality">
                                    <Image.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="3" Opacity="0.4"/>
                                    </Image.Effect>
                                </Image>

                                <Border Background="#AA000000" 
                                        Visibility="{Binding IsPlayable, Converter={StaticResource InvertedBoolToVisibilityConverter}}" 
                                        CornerRadius="5"
                                        IsHitTestVisible="False"/>
                            </Grid>

                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="CardVisual" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="CardVisual" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="CardVisual" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.Y)" To="0" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </VisualState>

                                    <VisualState x:Name="MouseOver">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="CardVisual" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" To="1.15" Duration="0:0:0.1">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation Storyboard.TargetName="CardVisual" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.Y)" To="-40" Duration="0:0:0.1">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </VisualState>

                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="CardVisual" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" To="1.1" Duration="0:0:0.05"/>
                                            <DoubleAnimation Storyboard.TargetName="CardVisual" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" To="1.1" Duration="0:0:0.05"/>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="ItemTemplate" DataType="{x:Type vm:ItemModel}">
        <Button Command="{Binding UseItemCommand}" 
                ToolTip="{Binding Type}"
                Background="Transparent"
                Width="50" Height="50" Margin="0,5"
                Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Image Source="{Binding ImagePath}" RenderOptions.BitmapScalingMode="HighQuality">
                            <Image.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="5" Opacity="0.5"/>
                            </Image.Effect>
                        </Image>

                        <Grid x:Name="BlockedOverlay" Visibility="Collapsed">
                            <Viewbox Stretch="Uniform" Margin="2">
                                <Grid>
                                    <Ellipse Fill="#AAFF0000" Width="100" Height="100"/>
                                    <TextBlock Text="🚫" Foreground="White" FontWeight="Bold" FontSize="90" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                            </Viewbox>
                        </Grid>

                        <Border x:Name="CounterBorder"
                                Background="#66000000"
                                BorderBrush="#AAFFFFFF"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="2,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                Margin="-5,-5">
                            <TextBlock Text="{Binding Count}" Foreground="White" FontWeight="Bold" FontSize="12"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Count}" Value="0">
                            <Setter TargetName="BlockedOverlay" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="CounterBorder" Property="Visibility" Value="Collapsed"/>
                            <Setter Property="Opacity" Value="0.7"/>
                        </DataTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.8"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </DataTemplate>

</ResourceDictionary>