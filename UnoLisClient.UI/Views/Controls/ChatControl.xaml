<UserControl x:Class="UnoLisClient.UI.Views.Controls.ChatControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UnoLisClient.UI.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

        <!-- 💬 Chat emergente -->
    <Border Background="#DD111111"
            CornerRadius="10"
            BorderBrush="White"
            BorderThickness="1"
            Padding="10"
            Width="280"
            Height="300">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer>
                <ItemsControl ItemsSource="{Binding ChatMessages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,3">
                                <TextBlock Text="{Binding Nickname}" Foreground="Gold" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Message}" Foreground="White" TextWrapping="Wrap"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <DockPanel Grid.Row="1" Margin="0,10,0,0">
                <TextBox x:Name="ChatInput" Width="200" Height="30" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <Button Content="Send"  Width="50" Height="30" Style="{StaticResource SecondaryButtonStyle}" Click="SendMessage_Click"/>
            </DockPanel>
        </Grid>
    </Border>
</UserControl>
