<UserControl x:Class="UnoLisClient.UI.Views.Controls.VisiblePasswordField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UnoLisClient.UI.Views.Controls"
             x:Name="Root">
    <Grid Height="40">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="35"/>
        </Grid.ColumnDefinitions>
        
        <PasswordBox x:Name="PasswordBox"
                     Grid.Column="0"
                     Style="{DynamicResource PasswordBoxStyle}"
                     Padding="5,0,10,0"
                     Height="35"
                     MaxWidth="500"
                     PasswordChanged="PasswordChanged"
                     MaxLength="255"/>
        <TextBox x:Name="VisiblePasswordTextBox"
                 Grid.Column="0"
                 Style="{DynamicResource TextBoxStyle}"
                 Padding="5,0,10,0"
                 Height="35"
                 MaxWidth="500"
                 Visibility="Collapsed"
                 TextChanged="VisiblePasswordTextChanged"
                 MaxLength="255"/>
        <Button x:Name="TogglePasswordButton"
                Grid.Column="1"
                Width="28"
                Height="28"
                Background="Transparent"
                BorderThickness="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Cursor="Hand"
                Margin="2,0,0,0"
                Click="ClickTogglePasswordButton">
            <Path x:Name="EyeIcon"
                  Data="M1,10 C4,2 12,2 15,10 C12,18 4,18 1,10 Z M8,13 A3,3 0 1 1 8,7 A3,3 0 1 1 8,13 Z"
                  Fill="#FFD369"
                  Stretch="Uniform"/>
        </Button>
    </Grid>
</UserControl>
