<UserControl x:Class="UnoLisClient.UI.Views.Controls.OpponentHandControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UnoLisClient.UI.Views.Controls"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:UnoLisClient.UI.ViewModels.ViewModelEntities"
             xmlns:converters="clr-namespace:UnoLisClient.UI.Converters"
             d:DataContext="{d:DesignInstance vm:OpponentModel}">
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:BoolToBrushConverter x:Key="TurnBorderBrushConverter" 
                                         TrueBrush="Gold" FalseBrush="Transparent" />
    </UserControl.Resources>

    <Border BorderBrush="{Binding IsTheirTurn, Converter={StaticResource TurnBorderBrushConverter}}" 
            BorderThickness="3" CornerRadius="10" Padding="5" Width="180">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">

            <Grid Width="120" Height="80" IsHitTestVisible="False">
                <Image Source="pack://application:,,,/Assets/Cards/BackUNOCard.png" Width="60" RenderTransformOrigin="0.5, 1">
                    <Image.RenderTransform>
                        <RotateTransform Angle="-20"/>
                    </Image.RenderTransform>
                </Image>
                <Image Source="pack://application:,,,/Assets/Cards/BackUNOCard.png" Width="60" RenderTransformOrigin="0.5, 1">
                    <Image.RenderTransform>
                        <RotateTransform Angle="-10"/>
                    </Image.RenderTransform>
                </Image>
                <Image Source="pack://application:,,,/Assets/Cards/BackUNOCard.png" Width="60" />
                <Image Source="pack://application:,,,/Assets/Cards/BackUNOCard.png" Width="60" RenderTransformOrigin="0.5, 1">
                    <Image.RenderTransform>
                        <RotateTransform Angle="10"/>
                    </Image.RenderTransform>
                </Image>
                <Image Source="pack://application:,,,/Assets/Cards/BackUNOCard.png" Width="60" RenderTransformOrigin="0.5, 1">
                    <Image.RenderTransform>
                        <RotateTransform Angle="20"/>
                    </Image.RenderTransform>
                </Image>
            </Grid>

            <Grid Margin="0,-30,0,0">
                <Ellipse Width="60" Height="60" Stroke="White" StrokeThickness="2">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Opacity" Value="1.0" />
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <BlurEffect Radius="0"/>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="False">
                                    <Setter Property="Opacity" Value="0.4" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding AvatarImagePath}" Stretch="UniformToFill"/>
                    </Ellipse.Fill>
                </Ellipse>

                <TextBlock Text="❌" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Foreground="Red">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="False">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <Grid Width="60" Height="60" Margin="0,-60,0,0" 
                  Visibility="{Binding IsSkipped, Converter={StaticResource BoolToVisibilityConverter}}">
                <Ellipse Fill="#AAFF0000" />
                <TextBlock Text="🚫" FontSize="40" Foreground="White" 
                           HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
            </Grid>

            <Border Background="#80000000" CornerRadius="5" Padding="5" Margin="0,5,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,0">
                    <Image Source="/Assets/Other/LobbyHost.png" Width="16" Height="16" Margin="0,0,5,0"
                        Visibility="{Binding IsHost, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <TextBlock Text="{Binding Nickname}" Foreground="White" />
                    <TextBlock Text=" (" Foreground="Gold" />
                    <TextBlock Text="{Binding CardCount}" Foreground="Gold" FontWeight="Bold"/>
                    <TextBlock Text=")" Foreground="Gold"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</UserControl>